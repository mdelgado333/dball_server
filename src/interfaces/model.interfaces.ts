export interface userInfo {
    username: string;
    name: string;
    password: string;
}

export interface info {
    name: string,
    subtitle: string,
    description: string
}

export interface dballInfo {
    vertical: string,
    typeOfContent: string
}